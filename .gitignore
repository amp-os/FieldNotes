# FieldNotes — .gitignore
*.iml
.gradle/
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx/
# Ad-hoc adb logcat captures for debugging (see README)
fieldnotes-log*.txt
**/build/
# whisper.cpp is added as a git submodule; its working tree is tracked via .gitmodules
